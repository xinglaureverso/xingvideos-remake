type ShortData = {
  slug: string;
  title: string;
  file: string;
  username: string;
  music: string;
};

type Comment = {
  user: string;
  message: string;
  time: string;
};

// Futuro: lista de comentários e postagem via input com localStorage
